<?xml version="1.0" encoding="UTF-8"?>
<!--
  A2W 桥接专用 FastDDS 配置：ROS2 侧只走回环，不碰机器人控制网。

  为什么需要：
    A2W 的 192.168.123.0/24（交换机1）是官方文档里写明的“DDS控制信号”局域网；
    本机 ROS2 默认用 FastDDS 域 0，会把发现组播 239.255.0.1（含类型对象）发到
    这张网卡上——机器人侧用 CycloneDDS 0.10.2，跨实现类型对象会让它出问题，
    运控会落到“阻尼＝软急停”（官方：1001 / Damp()“最高优先级，用于突发情况下的急停”）。
    实测：不加隔离时 `ros2 topic echo` 会在机器人网卡上加入 239.255.0.1；
          加本配置后该网卡上不再有任何 DDS 组播（只 lo）。

  用法：
    export FASTDDS_DEFAULT_PROFILES_FILE=/path/to/fastdds_iso.xml
    直接 `source src/a2w_bridge/scripts/a2w_env.sh` 最省事。

  注意：
    * 旧发行版（Humble / FastDDS 2.x）只认已废弃的 FASTRTPS_DEFAULT_PROFILES_FILE；
      本机 Lyrical（FastDDS 3.6）用 FASTDDS_DEFAULT_PROFILES_FILE，
      这时再设 FASTRTPS_* 会每进程打一条 deprecation WARN，所以默认不设；
      旧版环境请 `A2W_LEGACY_FASTRTPS=1 source .../a2w_env.sh`。
    * 本机所有需要看 /a2w/* 话题的 ROS2 工具（rviz2、point_lio、ros2 CLI 等）
      都要带上同样的环境变量 + 同一个 ROS_DOMAIN_ID，否则互相看不见；
    * 机器人数据不经过 ROS2 DDS（采集器 CycloneDDS → TCP → 节点），所以隔离后
      桥接功能不受影响；
    * FastDDS 2.x（如 Humble）把 <interfaceWhiteList> 放在 <participant><rtps> 下，
      3.x（本机 Lyrical 是 3.6.2）放在 transport_descriptor 下，本文件按 3.x 写。
-->
<dds xmlns="http://www.eprosima.com">
  <profiles>
    <transport_descriptors>
      <transport_descriptor>
        <transport_id>a2w_lo_only</transport_id>
        <type>UDPv4</type>
        <interfaceWhiteList>
          <address>127.0.0.1</address>
        </interfaceWhiteList>
      </transport_descriptor>
    </transport_descriptors>
    <participant profile_name="a2w_lo_only_participant" is_default_profile="true">
      <rtps>
        <useBuiltinTransports>false</useBuiltinTransports>
        <userTransports>
          <transport_id>a2w_lo_only</transport_id>
        </userTransports>
      </rtps>
    </participant>
  </profiles>
</dds>
